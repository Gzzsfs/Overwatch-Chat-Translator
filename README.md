# Overwatch-Chat-Translator
Windows desktop OCR overlay for translating Overwatch chat messages. The app captures only the selected screen region, recognizes chat text with a local PaddleOCR sidecar, classifies player messages, and displays translated Chinese text in an overlay. It does not read game memory, inject code, simulate input, or upload screenshots.
